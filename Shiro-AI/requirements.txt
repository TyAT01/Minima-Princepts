# ── Shiro AI — Requirements ────────────────────────────────────────────────
# Install with: pip install -r requirements.txt

# Core
gradio>=4.0.0
pyyaml>=6.0
requests>=2.31.0
aiohttp>=3.9.0
torch
Pillow
mss
googlesearch-python
pip install psutil

# LLM / AI
ollama
kokoro-onnx
onnxruntime-gpu
sounddevice
soundfile

# Memory / Vector store
chromadb>=0.4.0

# STT (Speech-to-Text)
faster-whisper>=0.10.0
# Silero VAD is loaded via torch.hub (no pip install needed — downloaded on first run)
# webrtcvad removed: replaced by Silero VAD (neural, more accurate, no false positives)

# Audio resampling — soxr (replaces scipy.signal.resample: higher quality, faster)
soxr

# Screen Vision — EasyOCR (replaces moondream2: no VRAM, reads on-screen text instantly)
# moondream2 deps removed: transformers, accelerate, einops (were ~2GB, never worked)
easyocr

# ── Discord ─────────────────────────────────────────────────────────────────
# Finalized March 2026 for DAVE E2EE Compliance (Hearing + Speaking)
#
discord.py[voice]>=2.7.1
PyNaCl>=1.5.0

# DAVE Encryption Handshake (Required to "Hear" after March 2nd update)
davey>=0.1.4

# Voice receiving (Using the patched dev-branch for DAVE stability)
# This replaces the broken PyPI version to fix the "Deaf Bot" issue.
git+https://github.com/imayhaveborkedit/discord-ext-voice-recv.git

# Windows Audio Support: 
# Only required if you upgrade to Python 3.13+. 
# Since you're on a lower version, the built-in 'audioop' works fine.
# audioop-lts  <-- Keep this commented out for now!

# Numpy (used for audio processing in voice receive)
numpy>=1.24.0

# ── YouTube Chat (DISABLED — install when ready to stream) ──────────────────
# pip install pytchat
# Then set youtube_chat.enabled: true in config.yaml